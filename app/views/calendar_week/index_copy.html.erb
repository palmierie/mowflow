<div class="row">
  <%= render :partial => 'partials/dashboard' %>

  <div class="col-sm-8 offset-md-1">

      <div id="jobs" class="row"> 
          <% @dates.each do |date|%>
          
          <%= date %>
          <% end %>
        <div id="left-col" class="list-group col-sm-2 connected-list">
          <% @jobs.each do |job|%>
            <%= link_to job.location_desc, job, class: "list-group-item"%>
          <%end%>
        </div>
        <div id="right-col" class="list-group col-sm-2 connected-list">
          <% @jobs.each do |job|%>
            <%= link_to job.location_desc, job, class: "list-group-item"%>
          <%end%>
        </div>
        
      </div>

     

  </div>
</div>